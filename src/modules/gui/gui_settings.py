"""Small class to hold gui settings common to all windows"""

class GuiSettings():
    """static class with variables that are shared across pages/frames
    """
    window_title = "X4 Save Manager"
    # window_controller sets the icon path
    icon_path = None